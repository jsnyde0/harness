[project]
name = "web-fetch"
version = "0.1.0"
description = "Token-friendly public web fetch via Jina, Firecrawl, or direct fetch"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]
